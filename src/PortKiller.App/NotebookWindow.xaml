<Window x:Class="PortKiller.App.NotebookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="📔 수첩"
        Width="400" Height="560"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterScreen"
        Background="#FFF7F7F4"
        FontFamily="Malgun Gothic"
        FontSize="13">

    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="SectionTitle">
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Foreground" Value="#FF2F4F2A" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>
        <Style TargetType="Border" x:Key="Card">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#FFE0E0DA" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Padding" Value="14" />
            <Setter Property="Margin" Value="0,0,0,12" />
        </Style>
    </Window.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
        <StackPanel>

            <!-- 오늘 할 일 -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Style="{StaticResource SectionTitle}" Text="오늘 할 일 3개" />
                    <TextBlock x:Name="TodoHint"
                               Foreground="#FF8A8A80"
                               FontSize="11"
                               Margin="0,0,0,10"
                               Text="세 개를 다 끝내면 펫이 축하해줘요" />

                    <StackPanel x:Name="TodoRows" />
                </StackPanel>
            </Border>

            <!-- 리마인더 -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Style="{StaticResource SectionTitle}" Text="리마인더" />

                    <ItemsControl x:Name="ReminderList" Margin="0,0,0,10" />

                    <TextBlock x:Name="ReminderEmpty"
                               Foreground="#FF8A8A80"
                               FontSize="11"
                               Margin="0,0,0,10"
                               Text="아직 등록한 알림이 없어요" />

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="ReminderText" Grid.Column="0" Padding="6,4"
                                 VerticalContentAlignment="Center" ToolTip="알림 내용" />
                        <TextBox x:Name="ReminderTime" Grid.Column="1" Width="58" Margin="6,0,0,0"
                                 Padding="6,4" Text="14:00" VerticalContentAlignment="Center"
                                 ToolTip="시각 (HH:MM)" />
                        <ComboBox x:Name="ReminderRepeatBox" Grid.Column="2" Width="76" Margin="6,0,0,0"
                                  SelectedIndex="0">
                            <ComboBoxItem Content="한 번" />
                            <ComboBoxItem Content="매일" />
                            <ComboBoxItem Content="평일" />
                        </ComboBox>
                        <Button x:Name="AddReminder" Grid.Column="3" Content="추가" Width="52"
                                Margin="6,0,0,0" Padding="0,4" Click="AddReminder_Click" />
                    </Grid>
                    <TextBlock x:Name="ReminderError" Foreground="#FFC0392B" FontSize="11"
                               Margin="0,6,0,0" Visibility="Collapsed" />
                </StackPanel>
            </Border>

            <!-- 뽀모도로 -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Style="{StaticResource SectionTitle}" Text="집중 (뽀모도로)" />

                    <TextBlock x:Name="PomodoroPhaseText"
                               FontSize="12" Foreground="#FF5A5A50" Text="쉬는 중" />
                    <TextBlock x:Name="PomodoroClock"
                               FontSize="34" FontWeight="Bold" Foreground="#FF2F4F2A"
                               Margin="0,2,0,8" Text="25:00" />

                    <StackPanel Orientation="Horizontal">
                        <Button x:Name="PomodoroButton" Content="집중 시작" Width="96" Padding="0,6"
                                Click="PomodoroButton_Click" />
                        <TextBlock x:Name="PomodoroCount" VerticalAlignment="Center" Margin="12,0,0,0"
                                   Foreground="#FF8A8A80" FontSize="11" Text="오늘까지 0회" />
                    </StackPanel>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Window>
